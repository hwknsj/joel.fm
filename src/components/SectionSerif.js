import styled from '@emotion/styled'

export const SectionSerif = styled.section`
  font-family: ${({ theme }) => theme.typeography.serif};
  p {
    font-family: ${({ theme }) => theme.typeography.serif};
  }
`
